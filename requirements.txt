# Direct dependencies — install with: pip install -r requirements.txt
Django==5.2.15
gunicorn==26.0.0
Pillow==12.3.0
python-dotenv==1.2.2

# Production / Render
dj-database-url==3.1.2
psycopg2-binary==2.9.12
whitenoise==6.12.0

# Cloudinary — production media file storage (used when DEBUG=False)
cloudinary==1.45.0
django-cloudinary-storage==0.3.0
